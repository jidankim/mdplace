{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:mdplace:schema:local-adapter-fixture-manifest:v1",
  "title": "mdplace Local Intelligence Adapter fixture manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schema_id", "manifest_id", "profile_id", "requirements", "fixtures"],
  "properties": {
    "$schema": {"const": "../schemas/local-adapter-fixture-manifest.schema.json"},
    "schema_id": {"const": "mdplace.local-adapter-fixture-manifest/v1"},
    "manifest_id": {"const": "local-adapter-fixtures:v1"},
    "profile_id": {"const": "local-adapter"},
    "requirements": {"type": "array", "minItems": 10, "maxItems": 10, "uniqueItems": true, "items": {"type": "string", "pattern": "^REQ-LIA-[0-9]{3}$"}},
    "fixtures": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/fixture"}}
  },
  "$defs": {
    "fixture": {"type": "object", "additionalProperties": false, "required": ["fixture_id", "path", "category", "requirement_ids"], "properties": {"fixture_id": {"type": "string", "pattern": "^FIX-LIA-PROFILE-[0-9]{3}$"}, "path": {"type": "string", "pattern": "^scenarios/local-intelligence-adapter/[a-z0-9][a-z0-9-]*\\.json$"}, "category": {"enum": ["positive", "negative", "exact_boundary", "over_boundary", "stale_state", "authority_denial", "illegal_transition", "crash_recovery"]}, "requirement_ids": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "pattern": "^REQ-LIA-[0-9]{3}$"}}}}
  }
}
